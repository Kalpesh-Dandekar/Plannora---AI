"use client";import {useState} from "react";import Link from "next/link";import {Check,LogOut,Save,UserRound} from "lucide-react";import {demoUser} from "@/lib/demo-data";import {PageHeader} from "./primitives";
export function SettingsView(){const [name,setName]=useState(demoUser.name),[email,setEmail]=useState(demoUser.email),[period,setPeriod]=useState("Evening"),[session,setSession]=useState("45"),[approach,setApproach]=useState("Balanced"),[mode,setMode]=useState("Exam"),[monday,setMonday]=useState(true),[saved,setSaved]=useState(false);const group=(label:string,values:string[],value:string,set:(x:string)=>void)=><fieldset className="settings-choice"><legend>{label}</legend><div>{values.map(x=><button type="button" aria-pressed={value===x} className={value===x?"active":""} onClick={()=>set(x)} key={x}>{value===x&&<Check/>}{x}</button>)}</div></fieldset>;return <><PageHeader eyebrow="YOUR PLANNORA" title="Settings" copy="Keep your profile and study preferences aligned with how you work."/><div className="settings-layout"><section className="settings-card"><header><UserRound/><div><h2>Profile</h2><p>Your local demo identity.</p></div></header><div className="settings-fields"><label>Name<input value={name} onChange={e=>setName(e.target.value)}/></label><label>Email<input type="email" value={email} onChange={e=>setEmail(e.target.value)}/></label></div></section><section className="settings-card"><header><Save/><div><h2>Study preferences</h2><p>Defaults for future plans and sessions.</p></div></header>{group("Preferred study period",["Morning","Afternoon","Evening","Night","Flexible"],period,setPeriod)}{group("Typical session",["25","45","60","Let Plannora decide"],session,setSession)}{group("Study approach",["Balanced","Concept-first","Practice-heavy","Revision-focused","Let Plannora decide"],approach,setApproach)}</section><section className="settings-card"><header><Save/><div><h2>Planner defaults</h2><p>Simple defaults for new plans.</p></div></header>{group("Default planning mode",["Regular","Exam"],mode,setMode)}<label className="toggle-row"><span><b>Week starts Monday</b><small>Use Monday as the first day in plan views.</small></span><button role="switch" aria-checked={monday} className={monday?"active":""} onClick={()=>setMonday(v=>!v)}><i/></button></label></section><div className="settings-actions"><button onClick={()=>setSaved(true)}><Save/>Save changes</button>{saved&&<span><Check/>Saved locally for this preview</span>}</div><section className="account-row"><div><h2>Account</h2><p>Authentication is not connected yet.</p></div><Link href="/"><LogOut/>Log out</Link></section></div></>}
